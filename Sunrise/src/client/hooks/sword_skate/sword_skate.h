#pragma once

namespace sunrise::client::hooks::sword_skate {

/** Clears the sword-air-attack glide refusal on the local player's jump press. */
void apply(void* component) noexcept;

} // namespace sunrise::client::hooks::sword_skate
